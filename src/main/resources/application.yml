spring:
  thymeleaf:
    cache: false
  jmx:
    enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/boot1?autoReconnect=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root

mybatis:
  configuration:
    mapUnderscoreToCamelCase: true
